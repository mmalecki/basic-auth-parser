on: pull_request
jobs:
  test:
    strategy:
      matrix:
        node: [0.6, 0.7, 18, latest]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}
      - run: npm run test
